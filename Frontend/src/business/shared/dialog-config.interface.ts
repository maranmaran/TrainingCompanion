
export interface DialogConfig {
    panelClass?: string[];
    duration?: number;
    data?: Object;
    height?: string,
    width?: string,
    maxWidth?: string,
    autoFocus?: false,
}

export const dialogDefaultConfig = {
};